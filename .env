# Development environment — do NOT commit this file
NODE_ENV=development
PORT=3000

# MongoDB — update with your Atlas connection string
DATABASE_URL="mongodb+srv://mobinulislam:8NWFTTL3vZqC2W0L@cluster0.mskd8ua.mongodb.net/enterprise_boilerplate?retryWrites=true&w=majority"

# JWT — replace with strong random secrets in production
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=dev_access_secret_replace_in_production_with_64_char_random_string
JWT_REFRESH_SECRET=dev_refresh_secret_replace_in_production_with_different_64_char_string
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

FRONTEND_URL=http://localhost:3000

# Email (optional — app works without it, email features are disabled)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM="Enterprise Backend <noreply@example.com>"

# Admin seed user
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=Admin@1234
